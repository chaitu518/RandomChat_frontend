# WebSocket endpoint used by the frontend (browser connects directly)
# Local frontend can connect to Railway backend via secure websocket:
VITE_WS_BROKER_URL=wss://randomchatbacken-production.up.railway.app/ws

# Alternate local backend example:
# VITE_WS_BROKER_URL=ws://localhost:8080/ws
